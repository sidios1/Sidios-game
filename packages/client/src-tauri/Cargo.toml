[package]
name = "juegos-rapidos"
version = "0.8.0"
description = "Sidios Game â€” plataforma de juegos de cartas LAN"
authors = ["Sidios"]
homepage = "https://sidios.cl"
edition = "2021"
rust-version = "1.77.2"

[lib]
# El nombre del crate de librerÃ­a que main.rs invoca (run()).
name = "juegos_rapidos_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
# Plugin shell: necesario para arrancar el sidecar del servidor LAN (Command.sidecar).
tauri-plugin-shell = "2"
