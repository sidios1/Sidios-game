{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos base de la ventana + ejecución del sidecar del servidor LAN embebido + diálogo nativo de carpeta (host de MeloQuiz) + lectura de subcarpetas para el checklist de categorías (MeloQuiz §11).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        {
          "name": "binaries/servidor",
          "sidecar": true,
          "args": []
        }
      ]
    },
    "shell:allow-kill",
    {
      "identifier": "fs:allow-read-dir",
      "allow": [{ "path": "**" }]
    }
  ]
}
