{
  "name": "juegos-rapidos",
  "version": "0.1.0",
  "private": true,
  "description": "Plataforma de juegos de cartas multijugador para escritorio",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "npm run test --workspaces --if-present",
    "dev:server": "npm run dev --workspace=@juegos/server",
    "dev:client": "npm run dev --workspace=@juegos/client"
  },
  "engines": {
    "node": ">=22"
  }
}
